# Testdata fixtures

`miniapp/` is a tiny in-tree fixture for indexer and compiler tests: TSX,
TypeScript, Python, CSS, Markdown, and a `.env` that must not be indexed.
Do not treat it as a real application.
